{
  "name": "rolldown-esm-external-require-plugin-issue",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "rm -rf dist-* && node --run build:vite && node --run build:rolldown",
    "build:vite": "vite build",
    "build:rolldown": "node rolldown.config.js",
    "preview": "node --run build && vite preview"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "vite": "8.0.0-beta.0",
    "rolldown": "1.0.0-beta.53"
  }
}
